name: 'React App'

on: 
  push:
    branches: [ "main" ]
    paths:
      - app/**
  pull_request:
    branches: [ "main" ]
    paths:
      - app/**

permissions:
  contents: read

jobs:
  build-and-test:
    name: 'Build and Test React App'
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v4

    # Setup Node.js environment
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: app/package-lock.json

    # Install dependencies
    - name: Install dependencies
      run: npm ci
      working-directory: ./app

    # # Run tests
    # - name: Run tests
    #   run: npm test -- --coverage --watchAll=false
    #   working-directory: ./app

    # Build the application
    - name: Build application
      run: npm run build
      working-directory: ./app

    # Configure AWS Credentials for S3 deployment
    - name: Configure AWS Credentials
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1  # Change to your desired region

    # Deploy to S3 bucket
    - name: Deploy to S3
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        aws s3 sync app/build/ s3://${{ secrets.S3_BUCKET_NAME }}/ --delete
      env:
        AWS_REGION: us-east-1  # Change to your desired region

    # # Optional: Invalidate CloudFront cache (if using CloudFront)
    # - name: Invalidate CloudFront
    #   if: github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.CLOUDFRONT_DISTRIBUTION_ID != ''
    #   run: |
    #     aws cloudfront create-invalidation --distribution-id ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }} --paths "/*"

    # # Optional: Upload build artifacts for backup/debugging
    # - name: Upload build artifacts
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: react-app-build
    #     path: app/build/